This is my C++ implementation of the red-black tree with NIL, it's no longer very far away from finished, it's actually done!



Use CMake with MSVC or clang to build it, or if you are just lazy, just go to the command line and type the compile command because there is only one file.



The python scripts are stress tests and tests to help narrow the problem down to a part in the input.

